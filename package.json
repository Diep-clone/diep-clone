{
  "name": "diep-clone_golang",
  "version": "1.0.0",
  "description": "diep.io clone ",
  "main": "index.js",
  "repository": "https://github.com/Diep-clone/diep-clone_golang",
  "license": "MIT",
  "private": false,
  "scripts": {
    "parcel:dev": "parcel watch public/index.html",
    "go:dev": "go run main.go",
    "parcel:build": "parcel build public/index.html --no-source-maps",
    "go:build": "go build main.go",
    "build": "npm run go:build && npm run parcel:build"
  },
  "devDependencies": {
  },
  "dependencies": {
  }
}
